from setuptools import setup
setup(
   name='mauka_api',
   version='1.0',
   description='A useful module',
   author='Vishal',
   author_email='vpa2013@outlook.com',
   packages=['mauka_api'],  #same as name
   # install_requires=['wheel', 'bar', 'greek'], #external packages as dependencies
)